-- CREATE Database BaesCouture;
-- USE BaesCouture;

-- CREATE TABLE user(user_id INT, username VARCHAR(50), pwd VARCHAR(50), firstName VARCHAR (50), 
-- lastName VARCHAR (50), isBuyer boolean, isSeller boolean, dob DATE, address VARCHAR(50), 
-- email VARCHAR(50), phone_no VARCHAR(13), payment_info VARCHAR(100), bank_account VARCHAR(50), rating int, PRIMARY KEY(user_id));

-- INSERT INTO user (user_id, username, pwd, firstName, lastName) VALUES 
-- (001, 'dBeckham', 'soccer103', 'David', 'Beckham'), 
-- (002, 'dMane', 'worldcup2026','Ousmane', 'Dembélé'), 
-- (003, 'neymarJr', '2026worldCup', 'Neymar', 'Jr');

SELECT * FROM user;